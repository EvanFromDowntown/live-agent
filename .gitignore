# Local secrets / env (never commit)
.env
.env.*

# SQLite databases created at runtime
*.db
*.db-journal
*.db-wal
*.db-shm

# Build artifacts
/agent
/bin/
*.log

# Sandbox scratch workspace (agent-generated code/output; default lives under
# $HOME/.liveagent, but ignore any stray copy created in the repo)
/sandbox_workspace/

# OS / editor
.DS_Store
.idea/
.vscode/
